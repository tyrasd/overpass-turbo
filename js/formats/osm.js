// osm data format module
turbo.formats.osm = {

    var format = {};

    format.match = function( data, meta ) {
        return true; // todo
    };

    format.toGeoJson = function( data ) {
        return; // ...
    };

    return format;
};